spring:
  datasource:
    url: jdbc:mysql://localhost:3306/golf_club?useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: examplepassword
  sql:
    init:
      mode: always
server:
  port: 8080
